# OPEN QUESTIONS

Unresolved decisions and what they block. Each is deferred to the task/stream noted; none is
an active blocker on dispatching `foundation`. Resolve before reaching the blocked task.

| # | Question | Blocks | Stream |
|---|----------|--------|--------|
| Q1 | ~~Exact packaging mechanics for a panel nested inside an app plugin (module registration vs a separate panel `plugin.json` under the app).~~ **RESOLVED (F4):** Nested-plugin pattern per Grafana docs — the panel lives at `src/panels/webview/` with its own `plugin.json` (id `wilsonwaters-webview-panel`) + sibling `module.tsx`; the `.config` webpack `getEntries()` discovers it and emits `dist/panels/webview/`, and Grafana registers it as a child plugin (no separate package.json/.config). The app `plugin.json` also declares an `includes` entry of `type: panel`. | F4 | foundation |
| Q2 | ~~Keep or remove the scaffold's example app pages (Page One–Four) once the Web View panel is registered.~~ **RESOLVED (F4):** Removed the demo Page One–Four, their routing (`utils.routing.ts`, `ROUTES`) and tests; kept the admin Configuration page and replaced the app root with a minimal `AppRoot` landing page. | F4 | foundation |
| Q3 | ~~How reliably config-vs-view mode can be detected from the panel editor context across the supported Grafana version range (`>=12.3.0`).~~ **RESOLVED (PC3):** Sidestepped — the interactive viewport positioning is a **custom panel options editor** (`addCustomEditor`, bound to `viewportZoom`; reads full options from `context.options` and writes X/Y/zoom). Grafana renders custom option editors only in the edit pane, so the panel component never needs edit-vs-view detection and view mode stays static/non-interactive. | PC1 | panel-core |
| Q4 | ~~Whether hide-selectors and the debug overlay can apply to cross-origin direct iframes at all (DOM access is blocked), or are only meaningful for same-origin proxied content.~~ **RESOLVED (PC5):** Debug overlay is rendered in our own DOM (not the iframe's) — always cross-origin-safe, implemented in PC5. `hideSelectors` requires CSS injection into the iframe DOM which is browser-blocked for cross-origin frames; the option is kept in the schema and will be applied server-side during HTML rewriting in proxy mode — see CR5. | PC5 / CR5 | panel-core / content-rewriting |
| Q5 | ~~Whether per-domain private-IP opt-in needs an audit-log hook in the security library or only at the consuming endpoint.~~ **RESOLVED (SF4):** Out of the security library — SF4 enforces the SF1 blocklist strictly with no opt-in parameter; the per-domain `AllowPrivateIP` opt-in (and any audit-log hook for it) lives at the consuming endpoint (proxy/frameability), not in `pkg/security`. | SF4 | security-foundation |
| Q6 | ~~Resolver behaviour when a hostname returns multiple A/AAAA records — validate all and dial the first valid, or fail closed if any record is denied.~~ **RESOLVED (SF4):** FAIL CLOSED — resolve all records, validate every one via SF1; if ANY is blocked, reject the whole request. Dial is pinned to a validated IP and `net.Dialer.Control` re-validates the exact connect IP (rebind defense). | SF4 | security-foundation |
| Q7 | ~~Whether `check-frameable` returns a `recommendedMode` for sites that error.~~ **RESOLVED (FR1):** security-pipeline DENIALS (bad scheme/port, not allowlisted, blocked IP, rate-limit) return proxy-style HTTP error codes (400/403/429) — surfaced as Error, no useful mode. When the pipeline PASSES, return HTTP 200 + `{frameable, reason, recommendedMode}`: `direct` if framable, `proxy` if framing is blocked (XFO/CSP) OR the upstream fetch errors/times out (treat ambiguous/error as proxy — the actionable suggestion, since the proxy strips framing and is more robust). | FR1 | frameability |
| Q8 | ~~What `/health` reports — bare liveness vs capability detail.~~ **RESOLVED (FR2):** BARE LIVENESS — 200 `{"status":"ok"}` (optionally a static plugin version), NO security pipeline, NO proxying, NO capability flags. DF1 infers "backend reachable ⇒ proxy available" (the plugin always serves `/proxy` when loaded). Capability detail can be added later if DF1 needs it. (Q12 covers the Cloud not-provisioned nuance for DF1.) | FR2 / DF1 | frameability / direct-only-fallback |
| Q9 | ~~Subresource-proxy URL scheme: how `/proxy` rewrites reference `/proxy-resource` — query-encoded absolute URL vs path-embedded.~~ **RESOLVED (CR2 design):** **query-encoded** — `/api/plugins/wilsonwaters-webview-app/resources/proxy-resource?url=<percent-encoded absolute URL>`, same shape as the top-level `/proxy?url=`. Chosen for unambiguous single-decode recovery in CR3 (path-embedding mangles `/ : ? # %` through the mux/`url.Parse`), clean non-interference with `<base href>` (rewritten refs are origin-absolute `/api/...` so base doesn't apply), and pipeline reuse (CR3 recovers via `Query().Get("url")` and runs SF2→SF3→SF5→SF4 verbatim). Rewriter builds the URL via the Go URL API (no string concat). | P1 / CR2 / CR3 | proxy / content-rewriting |
| Q10 | Whether the per-request timeout is one total budget or split into separate connect and total settings. | P4 | proxy |
| Q11 | ~~How aggressively to strip frame-buster JavaScript without breaking legitimate page scripts — needs a bounded, documented pattern set.~~ **RESOLVED (CR2 design):** static substring match on INLINE `<script>` text only (no JS execution, external scripts untouched); remove the whole inline script ONLY when it contains BOTH a self-vs-top comparison marker (`top != self`, `window.top !== window.self`, `window.frameElement`, `parent.frames.length`, …) AND a navigation/escape marker (`top.location =`, `top.location.replace`, `parent.location =`, …). False-NEGATIVE bias (miss a buster rather than break a legit script); documented marker table in code. Residual gaps (novel phrasings, external-JS busters) are accepted — header-level framing is already neutralised by `stripFramingHeaders`. | CR2 | content-rewriting |
| Q12 | On Grafana Cloud, what `/health` returns when the backend is simply not provisioned vs erroring, and whether backend availability can change mid-session (re-probe vs fixed per session). **RESOLVED (DF1 dispatch, 2026-06-09):** (a) **Fixed per session** — probe `/health` ONCE and cache at module scope (a single shared probe/promise across all panel-editor instances; no auto-poll). A live enable/disable mid-session is rare and out of scope; DF1 may expose an optional manual `refetch` but does not poll. (b) **Fail-safe interpretation** — `backendAvailable=true` ONLY on HTTP 200 with the FR2 liveness body (`{status:"ok"}`); ANY other outcome (network error, 404/503, timeout, unexpected body — i.e. not-provisioned, disabled, or erroring all look the same and need not be distinguished) ⇒ `false`. State machine: `loading` (initial) → `true` \| `false`; errors resolve to `false`, never a permanent `unknown` — degrading to direct-only is always safe. | DF1 | direct-only-fallback |
| Q13 | (a) ~~How to simulate DNS rebinding deterministically in CI for AC 21.~~ **RESOLVED (TC1 dispatch, 2026-06-09):** hermetically, with an injected stub `security.Resolver` (canned `net.IPAddr`, no network) — no production change needed (`security.NewDialer(resolver, base)` already takes an injectable resolver; `proxyHandler.transport` is a replaceable field; `ResolveAndValidate`/`NewControl` are exported). Three layers for AC-21 traceability: (1) **poisoned answer set** — stub returns one public + one private/link-local IP for a hostname → assert the WHOLE request fails closed per Q6 (`*DialError{Reason: ReasonBlockedIP}`, 403, `denials_total` increments, nothing dialled); (2) **TOCTOU rebind across resolve→connect** — stateful stub returns a benign public IP on the 1st lookup and a blocked IP on any subsequent lookup → assert the dialer connects ONLY to the validated first IP (recorded via a recording-transport/dialer), performs NO second lookup (counter==1), and `NewControl()`/`validateConnectAddr` independently rejects a blocked connect address; (3) **connect-time guard direct** — table test of `NewControl()` over loopback/link-local/metadata/public IPs. The AC-21 "end-to-end" requirement is met by driving (1)+(2) through the real `proxyHandler.ServeHTTP` with the stub-resolver transport and asserting HTTP status + metrics + that no blocked host is ever dialled. **(b) STILL OPEN:** whether the Grafana-version E2E matrix spans the full `>=12.3.0` range or a pinned subset for CI cost. | (a) TC1 ✅ / (b) TC6 | testing-cicd |
| Q14 | ~~Which Grafana instance `rootUrls` to sign for the initial private release (depends on the stakeholder's self-hosted target).~~ **RESOLVED (CI fix #78):** Both `ci.yml` and `release.yml` sign **privately** for the GitHub Actions environment `QA-Alintech` variable `GRAFANA_INSTANCE_URL` **+ `http://localhost:3000`** (so CI e2e validates). Token = repo secret `GRAFANA_ACCESS_POLICY_TOKEN`. To change the production target, edit the `GRAFANA_INSTANCE_URL` env variable — no code change. | DR8 | docs-release |
| Q15 | Whether the example dashboard's proxied URL should ship pre-allowlisted in provisioning for the demo, given the empty-by-default allowlist. | DR7 | docs-release |
| Q16 | Current catalog submission URL/review process (confirm at submission time), and whether Grafana Labs will accept a frame-header-stripping proxy on Cloud at all — be prepared to continue with Path 1 if rejected. | CP4 | catalog-prep |

| Q17 | In-panel PROXY render is blocked by Grafana's resource-route security headers (`X-Frame-Options: deny` + `CSP: sandbox` on `/api/plugins/*/resources/*`): pointing the iframe `src` at `/resources/proxy` (FR4) won't frame and proxied JS won't run. Fix approach = fetch-then-`srcdoc`. **RESOLVED (stakeholder, 2026-06-09): DEFER FR5 INDEFINITELY for v1.** Do NOT implement fetch-then-srcdoc now. Ship direct-mode + the backend proxy as-is; **in-panel PROXY-mode render is a documented KNOWN LIMITATION for v1** (the backend `/proxy` pipeline is complete + verified; only the in-panel rendering is deferred). TC4 SKIPS in-panel-proxy-render e2e assertions. Must be captured in docs (DR3 config guide + DR6 troubleshooting + README limitations). #102 stays open as a post-v1 follow-up; not a release blocker. | (deferred) FR5 #102 / docs | frameability |

| Q18 | Per-domain `AllowPrivateIP` opt-in is parsed + mapped (`config.go` → `security_map.go` → `security.EntryOptions`) but **never enforced** in the dial path. Spec tension: `brief.md` "hardcoded and not admin-configurable" vs AC 19 "unless per-domain private-IP opt-in is set." **RESOLVED (stakeholder, 2026-06-09): WIRE IT THROUGH.** Thread the matched domain's `AllowPrivateIP` into the dial path so the SF1 blocklist is relaxed **only for explicitly opted-in allowlisted domains** — deliberately re-opening a scoped SSRF surface. Must be carefully scoped (private/RFC1918 ranges only — NOT loopback/link-local/metadata, which should likely stay hard-blocked even when opted in — confirm in design), **audit-logged distinctly**, covered by dedicated tests (the AC 19 opt-in branch, mirroring TC1's strict-deny case), and documented in the admin guide + threat model. This is a security-critical change to `pkg/security` resolve-then-dial + the proxy/frameable call sites; needs design + an explicit adversarial security review. Tracked as issue **#105** (now decided/ready). | #105 / SF4-relax + proxy / DR4+DR5 docs | security-foundation |

## Notes

- Detailed threat-model / attack-technique content is intentionally NOT recorded here; it is a
  deferred documentation task (docs-release DR5).
